import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:spacex/app/modules/spacex/tabs/upcoming/upcoming_controller.dart';
import 'package:spacex/app/modules/spacex/spacex_module.dart';

void main() {
  initModule(SpacexModule());
  // UpcomingController upcoming;
  //
  setUp(() {
    //     upcoming = SpacexModule.to.get<UpcomingController>();
  });

  group('UpcomingController Test', () {
    //   test("First Test", () {
    //     expect(upcoming, isInstanceOf<UpcomingController>());
    //   });

    //   test("Set Value", () {
    //     expect(upcoming.value, equals(0));
    //     upcoming.increment();
    //     expect(upcoming.value, equals(1));
    //   });
  });
}
