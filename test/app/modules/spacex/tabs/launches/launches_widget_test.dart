import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:spacex/app/modules/spacex/tabs/launches/launches_widget.dart';

void main() {
//  testWidgets('LaunchesWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(LaunchesWidget()));
//    final textFinder = find.text('Launches');
//    expect(textFinder, findsOneWidget);
//  });
}
