import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:spacex/app/modules/spacex/tabs/launches/launches_controller.dart';
import 'package:spacex/app/modules/spacex/spacex_module.dart';

void main() {
  initModule(SpacexModule());
  // LaunchesController launches;
  //
  setUp(() {
    //     launches = SpacexModule.to.get<LaunchesController>();
  });

  group('LaunchesController Test', () {
    //   test("First Test", () {
    //     expect(launches, isInstanceOf<LaunchesController>());
    //   });

    //   test("Set Value", () {
    //     expect(launches.value, equals(0));
    //     launches.increment();
    //     expect(launches.value, equals(1));
    //   });
  });
}
