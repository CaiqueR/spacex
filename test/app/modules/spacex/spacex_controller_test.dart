import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:spacex/app/modules/spacex/spacex_controller.dart';
import 'package:spacex/app/modules/spacex/spacex_module.dart';

void main() {
  initModule(SpacexModule());
  // SpacexController spacex;
  //
  setUp(() {
    //     spacex = SpacexModule.to.get<SpacexController>();
  });

  group('SpacexController Test', () {
    //   test("First Test", () {
    //     expect(spacex, isInstanceOf<SpacexController>());
    //   });

    //   test("Set Value", () {
    //     expect(spacex.value, equals(0));
    //     spacex.increment();
    //     expect(spacex.value, equals(1));
    //   });
  });
}
