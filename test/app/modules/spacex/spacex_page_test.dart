import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:spacex/app/modules/spacex_page.dart';

void main() {
  testWidgets('SpacexPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SpacexPage(title: 'Spacex')));
    //  final titleFinder = find.text('Spacex');
    //  expect(titleFinder, findsOneWidget);
  });
}
